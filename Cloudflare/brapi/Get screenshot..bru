meta {
  name: Get screenshot.
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/accounts/:account_id/browser-rendering/screenshot
  body: json
  auth: apikey
}

params:query {
  ~cacheTTL: 
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
