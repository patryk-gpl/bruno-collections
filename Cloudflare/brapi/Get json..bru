meta {
  name: Get json.
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/browser-rendering/json
  body: json
  auth: apikey
}

params:query {
  ~cacheTTL: 
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
