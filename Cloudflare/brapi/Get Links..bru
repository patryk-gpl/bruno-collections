meta {
  name: Get Links.
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/accounts/:account_id/browser-rendering/links
  body: json
  auth: apikey
}

params:query {
  ~cacheTTL: 
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
