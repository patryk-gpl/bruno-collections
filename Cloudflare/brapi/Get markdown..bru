meta {
  name: Get markdown.
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/accounts/:account_id/browser-rendering/markdown
  body: json
  auth: apikey
}

params:query {
  ~cacheTTL: 
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
