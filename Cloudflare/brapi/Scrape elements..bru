meta {
  name: Scrape elements.
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/accounts/:account_id/browser-rendering/scrape
  body: json
  auth: apikey
}

params:query {
  ~cacheTTL: 
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
