meta {
  name: Delete a variant
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/images/v1/variants/:variant_id
  body: json
  auth: bearer
}

params:path {
  variant_id: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}
