meta {
  name: Delete TSIG
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/secondary_dns/tsigs/:tsig_id
  body: json
  auth: bearer
}

params:path {
  tsig_id: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}
