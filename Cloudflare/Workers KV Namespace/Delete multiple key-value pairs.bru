meta {
  name: Delete multiple key-value pairs
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/accounts/:account_id/storage/kv/namespaces/:namespace_id/bulk/delete
  body: json
  auth: bearer
}

params:path {
  namespace_id: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  [
    {}
  ]
}
