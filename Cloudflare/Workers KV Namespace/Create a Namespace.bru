meta {
  name: Create a Namespace
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/storage/kv/namespaces
  body: json
  auth: bearer
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": ""
  }
}
