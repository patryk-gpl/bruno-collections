meta {
  name: Update DNS Settings
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/zones/:zone_id/dns_settings
  body: json
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}
