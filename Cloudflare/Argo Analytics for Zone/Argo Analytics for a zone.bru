meta {
  name: Argo Analytics for a zone
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/zones/:zone_id/analytics/latency
  body: none
  auth: bearer
}

params:query {
  ~bins: 
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
