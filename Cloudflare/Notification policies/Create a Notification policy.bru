meta {
  name: Create a Notification policy
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/alerting/v3/policies
  body: json
  auth: bearer
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}
