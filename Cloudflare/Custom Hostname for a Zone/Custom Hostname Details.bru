meta {
  name: Custom Hostname Details
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/zones/:zone_id/custom_hostnames/:custom_hostname_id
  body: none
  auth: apikey
}

params:path {
  custom_hostname_id: 
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
