meta {
  name: List firewall rules
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/zones/:zone_id/firewall/rules
  body: none
  auth: apikey
}

params:query {
  ~description: 
  ~action: 
  ~page: 
  ~per_page: 
  ~id: 
  ~paused: 
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
