meta {
  name: Update priority of a firewall rule
  type: http
  seq: 8
}

patch {
  url: {{baseUrl}}/zones/:zone_id/firewall/rules/:rule_id
  body: json
  auth: apikey
}

params:path {
  rule_id: 
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "id": ""
  }
}
