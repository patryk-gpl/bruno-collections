meta {
  name: Delete project
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/pages/projects/:project_name
  body: json
  auth: bearer
}

params:path {
  project_name: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}
