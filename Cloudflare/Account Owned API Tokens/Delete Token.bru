meta {
  name: Delete Token
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/tokens/:token_id
  body: json
  auth: bearer
}

params:path {
  account_id: 
  token_id: 
}

auth:bearer {
  token: {{token}}
}
