meta {
  name: Update Token
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/accounts/:account_id/tokens/:token_id
  body: json
  auth: bearer
}

params:path {
  account_id: 
  token_id: 
}

auth:bearer {
  token: {{token}}
}
