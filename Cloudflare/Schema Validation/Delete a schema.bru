meta {
  name: Delete a schema
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/zones/:zone_id/schema_validation/schemas/:schema_id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
