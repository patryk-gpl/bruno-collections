meta {
  name: Upload a schema
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/zones/:zone_id/schema_validation/schemas
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "kind": "",
    "name": "",
    "source": "",
    "validation_enabled": false
  }
}
