meta {
  name: Retrieve all operations from the schema.
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/zones/:zone_id/schema_validation/schemas/:schema_id/operations
  body: none
  auth: bearer
}

params:query {
  ~feature: 
  ~host: 
  ~method: 
  ~endpoint: 
  ~page: 
  ~per_page: 
  ~operation_status: 
}

auth:bearer {
  token: {{token}}
}
