meta {
  name: Leave Organization
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/user/organizations/:organization_id
  body: json
  auth: apikey
}

params:path {
  organization_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
