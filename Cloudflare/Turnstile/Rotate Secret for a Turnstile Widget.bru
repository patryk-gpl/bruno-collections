meta {
  name: Rotate Secret for a Turnstile Widget
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/accounts/:account_id/challenges/widgets/:sitekey/rotate_secret
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "invalidate_immediately": false
  }
}
