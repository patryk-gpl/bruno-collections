meta {
  name: Update an Access application
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/zones/:zone_id/access/apps/:app_id
  body: json
  auth: apikey
}

params:path {
  app_id: 
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {}
}
