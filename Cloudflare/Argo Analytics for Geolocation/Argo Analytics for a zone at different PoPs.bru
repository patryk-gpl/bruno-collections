meta {
  name: Argo Analytics for a zone at different PoPs
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/zones/:zone_id/analytics/latency/colos
  body: none
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
