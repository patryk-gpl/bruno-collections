meta {
  name: Delete Health Check
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/zones/:zone_id/healthchecks/:healthcheck_id
  body: json
  auth: bearer
}

params:path {
  healthcheck_id: 
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
