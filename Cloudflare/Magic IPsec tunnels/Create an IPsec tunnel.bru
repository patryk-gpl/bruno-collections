meta {
  name: Create an IPsec tunnel
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/magic/ipsec_tunnels
  body: json
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
  ~x-magic-new-hc-target: 
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
