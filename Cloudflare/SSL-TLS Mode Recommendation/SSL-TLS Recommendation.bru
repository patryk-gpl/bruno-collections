meta {
  name: SSL/TLS Recommendation
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/zones/:zone_id/ssl/recommendation
  body: none
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
