meta {
  name: Delete an operation
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/zones/:zone_id/api_gateway/operations/:operation_id
  body: none
  auth: apikey
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
