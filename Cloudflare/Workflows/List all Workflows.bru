meta {
  name: List all Workflows
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/accounts/:account_id/workflows
  body: none
  auth: apikey
}

params:query {
  ~per_page: 
  ~page: 
  ~search: 
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
