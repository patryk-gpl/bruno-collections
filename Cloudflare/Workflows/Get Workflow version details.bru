meta {
  name: Get Workflow version details
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/accounts/:account_id/workflows/:workflow_name/versions/:version_id
  body: none
  auth: apikey
}

params:path {
  workflow_name: 
  version_id: 
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
