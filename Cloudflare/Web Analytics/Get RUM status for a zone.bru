meta {
  name: Get RUM status for a zone
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/zones/:zone_id/settings/rum
  body: none
  auth: apikey
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
