meta {
  name: Delete Version
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/workers/workers/:worker_id/versions/:version_id
  body: none
  auth: bearer
}

params:path {
  account_id: 
  worker_id: 
  version_id: 
}

auth:bearer {
  token: {{token}}
}
