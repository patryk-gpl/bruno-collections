meta {
  name: Get IP address details
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/radar/entities/ip
  body: none
  auth: apikey
}

params:query {
  ip: 
  ~format: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
