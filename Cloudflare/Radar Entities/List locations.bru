meta {
  name: List locations
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/radar/entities/locations
  body: none
  auth: apikey
}

params:query {
  ~limit: 
  ~offset: 
  ~location: 
  ~format: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
