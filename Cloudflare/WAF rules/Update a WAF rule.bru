meta {
  name: Update a WAF rule
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/zones/:zone_id/firewall/waf/packages/:package_id/rules/:rule_id
  body: json
  auth: apikey
}

params:path {
  rule_id: 
  package_id: 
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "mode": ""
  }
}
