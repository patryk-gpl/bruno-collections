meta {
  name: Update DNS Settings
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/accounts/:account_id/dns_settings
  body: json
  auth: bearer
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "zone_defaults": {}
  }
}
