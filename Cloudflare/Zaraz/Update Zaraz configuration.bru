meta {
  name: Update Zaraz configuration
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/zones/:zone_id/settings/zaraz/config
  body: json
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
