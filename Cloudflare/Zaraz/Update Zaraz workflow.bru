meta {
  name: Update Zaraz workflow
  type: http
  seq: 10
}

put {
  url: {{baseUrl}}/zones/:zone_id/settings/zaraz/workflow
  body: json
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
