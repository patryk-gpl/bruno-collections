meta {
  name: List a zone ruleset's versions
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/zones/:zone_id/rulesets/:ruleset_id/versions
  body: none
  auth: bearer
}

params:path {
  ruleset_id: 
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
