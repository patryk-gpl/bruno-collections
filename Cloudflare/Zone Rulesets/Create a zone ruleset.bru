meta {
  name: Create a zone ruleset
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/zones/:zone_id/rulesets
  body: json
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
