meta {
  name: Get a zone ruleset
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/zones/:zone_id/rulesets/:ruleset_id
  body: none
  auth: bearer
}

params:path {
  ruleset_id: 
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
