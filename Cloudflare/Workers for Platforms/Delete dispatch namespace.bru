meta {
  name: Delete dispatch namespace
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/workers/dispatch/namespaces/:dispatch_namespace
  body: none
  auth: bearer
}

params:path {
  account_id: 
  dispatch_namespace: 
}

auth:bearer {
  token: {{token}}
}
