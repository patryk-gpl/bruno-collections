meta {
  name: Get dispatch namespace
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/accounts/:account_id/workers/dispatch/namespaces/:dispatch_namespace
  body: none
  auth: bearer
}

params:path {
  account_id: 
  dispatch_namespace: 
}

auth:bearer {
  token: {{token}}
}
