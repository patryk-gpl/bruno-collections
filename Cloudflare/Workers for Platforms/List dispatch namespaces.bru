meta {
  name: List dispatch namespaces
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/accounts/:account_id/workers/dispatch/namespaces
  body: none
  auth: bearer
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}
