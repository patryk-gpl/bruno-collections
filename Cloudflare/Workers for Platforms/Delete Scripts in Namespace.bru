meta {
  name: Delete Scripts in Namespace
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/accounts/:account_id/workers/dispatch/namespaces/:dispatch_namespace/scripts
  body: none
  auth: bearer
}

params:query {
  ~tags: 
}

params:path {
  account_id: 
  dispatch_namespace: 
}

auth:bearer {
  token: {{token}}
}
