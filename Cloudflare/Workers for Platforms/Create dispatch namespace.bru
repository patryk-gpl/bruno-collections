meta {
  name: Create dispatch namespace
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/workers/dispatch/namespaces
  body: json
  auth: bearer
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": ""
  }
}
