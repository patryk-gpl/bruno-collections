meta {
  name: Delete Vectors By Identifier
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/accounts/:account_id/vectorize/v2/indexes/:index_name/delete_by_ids
  body: json
  auth: bearer
}

params:path {
  account_id: 
  index_name: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "ids": []
  }
}
