meta {
  name: Update Page Shield settings
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/zones/:zone_id/page_shield
  body: json
  auth: apikey
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
