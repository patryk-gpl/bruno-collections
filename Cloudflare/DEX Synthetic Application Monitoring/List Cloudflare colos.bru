meta {
  name: List Cloudflare colos
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/accounts/:account_id/dex/colos
  body: none
  auth: apikey
}

params:query {
  from: 
  to: 
  ~sortBy: 
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
