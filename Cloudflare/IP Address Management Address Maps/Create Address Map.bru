meta {
  name: Create Address Map
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/addressing/address_maps
  body: json
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
