meta {
  name: Remove an account membership from an Address Map
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/accounts/:account_id/addressing/address_maps/:address_map_id/accounts/:account_id
  body: json
  auth: apikey
}

params:path {
  account_id: 
  address_map_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
