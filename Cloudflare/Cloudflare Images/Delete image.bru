meta {
  name: Delete image
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/images/v1/:image_id
  body: json
  auth: bearer
}

params:path {
  image_id: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}
