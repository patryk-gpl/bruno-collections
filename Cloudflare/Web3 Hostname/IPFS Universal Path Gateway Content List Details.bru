meta {
  name: IPFS Universal Path Gateway Content List Details
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/zones/:zone_id/web3/hostnames/:identifier/ipfs_universal_path/content_list
  body: none
  auth: apikey
}

params:path {
  identifier: 
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
