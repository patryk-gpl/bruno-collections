meta {
  name: Get URL Normalization settings
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/zones/:zone_id/url_normalization
  body: none
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
