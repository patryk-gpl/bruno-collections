meta {
  name: Delete DNS Record
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/zones/:zone_id/dns_records/:dns_record_id
  body: json
  auth: bearer
}

params:path {
  dns_record_id: 
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
