meta {
  name: Import DNS Records
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/zones/:zone_id/dns_records/import
  body: multipartForm
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  file: 
  proxied: 
}
