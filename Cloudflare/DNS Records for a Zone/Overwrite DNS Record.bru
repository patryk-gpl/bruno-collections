meta {
  name: Overwrite DNS Record
  type: http
  seq: 6
}

put {
  url: {{baseUrl}}/zones/:zone_id/dns_records/:dns_record_id
  body: json
  auth: bearer
}

params:path {
  dns_record_id: 
  zone_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}
