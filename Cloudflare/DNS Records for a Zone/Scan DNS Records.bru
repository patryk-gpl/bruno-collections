meta {
  name: Scan DNS Records
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/zones/:zone_id/dns_records/scan
  body: json
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
