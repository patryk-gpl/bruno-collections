meta {
  name: Purge Cached Content
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/zones/:zone_id/purge_cache
  body: json
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
