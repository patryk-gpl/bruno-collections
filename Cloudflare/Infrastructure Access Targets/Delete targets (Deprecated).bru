meta {
  name: Delete targets (Deprecated)
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/accounts/:account_id/infrastructure/targets/batch
  body: json
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "target_ids": []
  }
}
