meta {
  name: Create Zone Hold
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/zones/:zone_id/hold
  body: none
  auth: bearer
}

params:query {
  ~include_subdomains: 
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
