meta {
  name: Remove Zone Hold
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/zones/:zone_id/hold
  body: none
  auth: bearer
}

params:query {
  ~hold_after: 
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
