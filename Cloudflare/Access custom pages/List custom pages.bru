meta {
  name: List custom pages
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/accounts/:account_id/access/custom_pages
  body: none
  auth: apikey
}

params:query {
  ~page: 
  ~per_page: 
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
