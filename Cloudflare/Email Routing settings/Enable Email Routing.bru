meta {
  name: Enable Email Routing
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/zones/:zone_id/email/routing/enable
  body: json
  auth: apikey
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
