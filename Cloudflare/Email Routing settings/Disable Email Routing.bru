meta {
  name: Disable Email Routing
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/zones/:zone_id/email/routing/dns
  body: json
  auth: apikey
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "name": ""
  }
}
