meta {
  name: List tested webpages
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/zones/:zone_id/speed_api/pages
  body: none
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
