meta {
  name: Get Worker
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/accounts/:account_id/workers/workers/:worker_id
  body: none
  auth: bearer
}

params:path {
  account_id: 
  worker_id: 
}

auth:bearer {
  token: {{token}}
}
