meta {
  name: Add Custom Scan Expressions
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/zones/:zone_id/content-upload-scan/payloads
  body: json
  auth: apikey
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  [
    {
      "payload": ""
    }
  ]
}
