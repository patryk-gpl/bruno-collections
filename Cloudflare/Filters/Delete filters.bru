meta {
  name: Delete filters
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/zones/:zone_id/filters
  body: none
  auth: apikey
}

params:query {
  id: 
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
