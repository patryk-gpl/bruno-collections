meta {
  name: Delete Spectrum application
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/zones/:zone_id/spectrum/apps/:app_id
  body: none
  auth: bearer
}

params:path {
  app_id: 
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
