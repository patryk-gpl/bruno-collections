meta {
  name: Get HTTP authentication requests by compromised credential status time series
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/radar/leaked_credential_checks/timeseries_groups/compromised
  body: none
  auth: apikey
}

params:query {
  ~aggInterval: 
  ~name: 
  ~dateRange: 
  ~dateStart: 
  ~dateEnd: 
  ~botClass: 
  ~format: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
