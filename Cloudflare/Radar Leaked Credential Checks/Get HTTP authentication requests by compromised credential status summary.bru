meta {
  name: Get HTTP authentication requests by compromised credential status summary
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/radar/leaked_credential_checks/summary/compromised
  body: none
  auth: apikey
}

params:query {
  ~name: 
  ~dateRange: 
  ~dateStart: 
  ~dateEnd: 
  ~botClass: 
  ~format: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
