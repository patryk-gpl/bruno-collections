meta {
  name: Get HTTP authentication requests by bot class time series
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/radar/leaked_credential_checks/timeseries_groups/bot_class
  body: none
  auth: apikey
}

params:query {
  ~aggInterval: 
  ~name: 
  ~dateRange: 
  ~dateStart: 
  ~dateEnd: 
  ~compromised: 
  ~format: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
