meta {
  name: Get IP Lists
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/accounts/:account_id/intel/ip-list
  body: none
  auth: bearer
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}
