meta {
  name: Create deployment
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/pages/projects/:project_name/deployments
  body: multipartForm
  auth: bearer
}

params:path {
  project_name: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  branch: 
}
