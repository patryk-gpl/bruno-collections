meta {
  name: Delete Internal DNS View
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/dns_settings/views/:view_id
  body: json
  auth: bearer
}

params:path {
  account_id: 
  view_id: 
}

auth:bearer {
  token: {{token}}
}
