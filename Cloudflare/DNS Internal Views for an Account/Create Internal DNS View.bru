meta {
  name: Create Internal DNS View
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/dns_settings/views
  body: json
  auth: bearer
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}
