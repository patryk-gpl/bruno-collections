meta {
  name: Update verification statuses of tracked URLs to confirmed by ID
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/accounts/:account_id/brand-protection/alerts/verify
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
