meta {
  name: Update alerts on submitted domains by ID
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/accounts/:account_id/brand-protection/alerts
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
