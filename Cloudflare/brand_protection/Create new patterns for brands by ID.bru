meta {
  name: Create new patterns for brands by ID
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/accounts/:account_id/brand-protection/brands/patterns
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
