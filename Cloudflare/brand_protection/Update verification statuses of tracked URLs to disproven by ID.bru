meta {
  name: Update verification statuses of tracked URLs to disproven by ID
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/accounts/:account_id/brand-protection/alerts/refute
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
