meta {
  name: Update verification statuses of submitted URLs to confirmed by ID
  type: http
  seq: 20
}

patch {
  url: {{baseUrl}}/accounts/:account_id/brand-protection/verify
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
