meta {
  name: Update verification statuses of tracked URLs to awaiting by ID
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/accounts/:account_id/brand-protection/alerts/clear
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
