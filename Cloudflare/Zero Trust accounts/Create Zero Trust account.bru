meta {
  name: Create Zero Trust account
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/accounts/:account_id/gateway
  body: none
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
