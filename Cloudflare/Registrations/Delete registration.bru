meta {
  name: Delete registration
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/accounts/:account_id/devices/registrations/:registration_id
  body: none
  auth: bearer
}

params:path {
  registration_id: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}
