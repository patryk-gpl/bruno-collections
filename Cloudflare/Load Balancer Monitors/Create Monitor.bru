meta {
  name: Create Monitor
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/user/load_balancers/monitors
  body: json
  auth: apikey
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
