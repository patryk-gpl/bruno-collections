meta {
  name: Edit Universal SSL Settings
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/zones/:zone_id/ssl/universal/settings
  body: json
  auth: apikey
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "enabled": false
  }
}
