meta {
  name: Create an account ruleset rule
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/accounts/:account_id/rulesets/:ruleset_id/rules
  body: json
  auth: bearer
}

params:path {
  ruleset_id: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}
