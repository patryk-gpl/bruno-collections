meta {
  name: Create an account ruleset
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/rulesets
  body: json
  auth: bearer
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}
