meta {
  name: List account rulesets
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/accounts/:account_id/rulesets
  body: none
  auth: bearer
}

params:query {
  ~cursor: 
  ~per_page: 
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}
