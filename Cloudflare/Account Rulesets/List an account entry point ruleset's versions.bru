meta {
  name: List an account entry point ruleset's versions
  type: http
  seq: 15
}

get {
  url: {{baseUrl}}/accounts/:account_id/rulesets/phases/:ruleset_phase/entrypoint/versions
  body: none
  auth: bearer
}

params:path {
  ruleset_phase: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}
