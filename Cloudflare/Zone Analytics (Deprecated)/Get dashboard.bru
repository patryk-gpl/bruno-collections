meta {
  name: Get dashboard
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/zones/:zone_identifier/analytics/dashboard
  body: none
  auth: apikey
}

params:query {
  ~until: 
  ~since: 
  ~continuous: 
}

params:path {
  zone_identifier: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
