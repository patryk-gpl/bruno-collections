meta {
  name: Delete an email allow policy
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/email-security/settings/allow_policies/:policy_id
  body: none
  auth: apikey
}

params:path {
  account_id: 
  policy_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
