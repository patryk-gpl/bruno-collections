meta {
  name: Delete Waiting Room Rule
  type: http
  seq: 18
}

delete {
  url: {{baseUrl}}/zones/:zone_id/waiting_rooms/:waiting_room_id/rules/:rule_id
  body: json
  auth: bearer
}

params:path {
  rule_id: 
  waiting_room_id: 
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
