meta {
  name: Delete event
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/zones/:zone_id/waiting_rooms/:waiting_room_id/events/:event_id
  body: json
  auth: bearer
}

params:path {
  event_id: 
  waiting_room_id: 
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
