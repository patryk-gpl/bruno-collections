meta {
  name: Update zone-level Waiting Room settings
  type: http
  seq: 24
}

put {
  url: {{baseUrl}}/zones/:zone_id/waiting_rooms/settings
  body: json
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "search_engine_crawler_bypass": false
  }
}
