meta {
  name: List waiting rooms for zone
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/zones/:zone_id/waiting_rooms
  body: none
  auth: bearer
}

params:query {
  ~page: 
  ~per_page: 
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
