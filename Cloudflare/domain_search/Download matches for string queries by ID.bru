meta {
  name: Download matches for string queries by ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/accounts/:account_id/brand-protection/matches/download
  body: none
  auth: bearer
}

params:query {
  ~id: 
  ~offset: 
  ~limit: 
  ~include_domain_id: 
}

auth:bearer {
  token: {{token}}
}
