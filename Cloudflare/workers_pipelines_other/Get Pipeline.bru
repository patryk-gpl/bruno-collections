meta {
  name: Get Pipeline
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/accounts/:account_id/pipelines/:pipeline_name
  body: none
  auth: bearer
}

params:path {
  account_id: 
  pipeline_name: 
}

auth:bearer {
  token: {{token}}
}
