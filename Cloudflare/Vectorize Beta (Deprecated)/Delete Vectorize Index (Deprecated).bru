meta {
  name: Delete Vectorize Index (Deprecated)
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/vectorize/indexes/:index_name
  body: none
  auth: bearer
}

params:path {
  account_id: 
  index_name: 
}

auth:bearer {
  token: {{token}}
}
