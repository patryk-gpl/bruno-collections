meta {
  name: List Vectorize Indexes (Deprecated)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/accounts/:account_id/vectorize/indexes
  body: none
  auth: bearer
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}
