meta {
  name: Update a webhook
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/accounts/:account_id/alerting/v3/destinations/webhooks/:webhook_id
  body: json
  auth: bearer
}

params:path {
  webhook_id: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}
