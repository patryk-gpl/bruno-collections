meta {
  name: Create a webhook
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/alerting/v3/destinations/webhooks
  body: json
  auth: bearer
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}
