meta {
  name: List Tokens
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/user/tokens
  body: none
  auth: bearer
}

params:query {
  ~page: 
  ~per_page: 
  ~direction: 
}

auth:bearer {
  token: {{token}}
}
