meta {
  name: Rotate Zero Trust SSH Account Seed
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/accounts/:account_id/gateway/audit_ssh_settings/rotate_seed
  body: none
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
