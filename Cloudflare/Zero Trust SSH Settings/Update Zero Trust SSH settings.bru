meta {
  name: Update Zero Trust SSH settings
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/accounts/:account_id/gateway/audit_ssh_settings
  body: json
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "public_key": ""
  }
}
