meta {
  name: Create a proxy endpoint
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/gateway/proxy_endpoints
  body: json
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "ips": [],
    "name": ""
  }
}
