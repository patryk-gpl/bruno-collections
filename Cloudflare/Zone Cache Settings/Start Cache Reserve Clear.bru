meta {
  name: Start Cache Reserve Clear
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/zones/:zone_id/cache/cache_reserve_clear
  body: json
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
