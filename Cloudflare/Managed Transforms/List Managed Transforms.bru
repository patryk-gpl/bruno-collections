meta {
  name: List Managed Transforms
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/zones/:zone_id/managed_headers
  body: none
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
