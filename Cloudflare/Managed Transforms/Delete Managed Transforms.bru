meta {
  name: Delete Managed Transforms
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/zones/:zone_id/managed_headers
  body: none
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
