meta {
  name: Creates event references for a event
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/accounts/:account_id/cloudforce-one/events/relate/:event_id/create
  body: json
  auth: bearer
}

params:path {
  account_id: 
  event_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "events": []
  }
}
