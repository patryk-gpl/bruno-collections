meta {
  name: Removes a tag from an event
  type: http
  seq: 16
}

delete {
  url: {{baseUrl}}/accounts/:account_id/cloudforce-one/events/event_tag/:event_id
  body: json
  auth: bearer
}

params:path {
  account_id: 
  event_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "tags": []
  }
}
