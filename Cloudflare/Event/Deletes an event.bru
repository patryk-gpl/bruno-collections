meta {
  name: Deletes an event
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/accounts/:account_id/cloudforce-one/events/:event_id
  body: none
  auth: bearer
}

params:path {
  account_id: 
  event_id: 
}

auth:bearer {
  token: {{token}}
}
