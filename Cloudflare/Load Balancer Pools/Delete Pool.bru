meta {
  name: Delete Pool
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/user/load_balancers/pools/:pool_id
  body: json
  auth: apikey
}

params:path {
  pool_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
