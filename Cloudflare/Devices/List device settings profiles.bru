meta {
  name: List device settings profiles
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/accounts/:account_id/devices/policies
  body: none
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
