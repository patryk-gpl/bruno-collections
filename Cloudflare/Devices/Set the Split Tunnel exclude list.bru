meta {
  name: Set the Split Tunnel exclude list
  type: http
  seq: 18
}

put {
  url: {{baseUrl}}/accounts/:account_id/devices/policy/exclude
  body: json
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  [
    {}
  ]
}
