meta {
  name: Revoke devices (deprecated)
  type: http
  seq: 23
}

post {
  url: {{baseUrl}}/accounts/:account_id/devices/revoke
  body: json
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  [
    {}
  ]
}
