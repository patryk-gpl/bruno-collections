meta {
  name: Get Certificate Details
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/zones/:zone_id/origin_tls_client_auth/:certificate_id
  body: none
  auth: apikey
}

params:path {
  certificate_id: 
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
