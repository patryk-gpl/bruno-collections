meta {
  name: Upload Certificate
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/zones/:zone_id/origin_tls_client_auth
  body: json
  auth: apikey
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "certificate": "",
    "private_key": ""
  }
}
