meta {
  name: Update Membership
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/memberships/:membership_id
  body: json
  auth: apikey
}

params:path {
  membership_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
