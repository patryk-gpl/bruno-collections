meta {
  name: Delete Membership
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/memberships/:membership_id
  body: json
  auth: apikey
}

params:path {
  membership_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
