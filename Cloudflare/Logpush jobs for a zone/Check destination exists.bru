meta {
  name: Check destination exists
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/zones/:zone_id/logpush/validate/destination/exists
  body: json
  auth: apikey
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "destination_conf": ""
  }
}
