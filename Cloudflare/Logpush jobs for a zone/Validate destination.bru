meta {
  name: Validate destination
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/zones/:zone_id/logpush/validate/destination
  body: json
  auth: apikey
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "destination_conf": ""
  }
}
