meta {
  name: Delete ACL
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/secondary_dns/acls/:acl_id
  body: json
  auth: bearer
}

params:path {
  acl_id: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}
