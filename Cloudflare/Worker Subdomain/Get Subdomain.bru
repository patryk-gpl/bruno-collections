meta {
  name: Get Subdomain
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/accounts/:account_id/workers/subdomain
  body: none
  auth: bearer
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}
