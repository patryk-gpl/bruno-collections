meta {
  name: Get Route
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/zones/:zone_id/workers/routes/:route_id
  body: none
  auth: bearer
}

params:path {
  route_id: 
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
