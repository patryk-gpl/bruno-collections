meta {
  name: List Routes
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/zones/:zone_id/workers/routes
  body: none
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
