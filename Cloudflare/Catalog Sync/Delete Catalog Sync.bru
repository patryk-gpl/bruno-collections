meta {
  name: Delete Catalog Sync
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/magic/cloud/catalog-syncs/:sync_id
  body: none
  auth: apikey
}

params:query {
  ~delete_destination: 
}

params:path {
  account_id: 
  sync_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
