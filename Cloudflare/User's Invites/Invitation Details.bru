meta {
  name: Invitation Details
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/user/invites/:invite_id
  body: none
  auth: apikey
}

params:path {
  invite_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
