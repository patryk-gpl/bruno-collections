meta {
  name: Respond to Invitation
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/user/invites/:invite_id
  body: json
  auth: apikey
}

params:path {
  invite_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "status": ""
  }
}
