meta {
  name: Edit SSL Certificate Pack Validation Method
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/zones/:zone_id/ssl/verification/:certificate_pack_id
  body: json
  auth: apikey
}

params:path {
  certificate_pack_id: 
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "validation_method": ""
  }
}
