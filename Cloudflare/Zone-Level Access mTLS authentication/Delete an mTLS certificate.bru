meta {
  name: Delete an mTLS certificate
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/zones/:zone_id/access/certificates/:certificate_id
  body: none
  auth: apikey
}

params:path {
  certificate_id: 
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
