meta {
  name: Delete saved logo queries by ID
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/accounts/:account_id/brand-protection/logos/:logo_id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
