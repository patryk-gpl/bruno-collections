meta {
  name: Create DNS Firewall Cluster
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/dns_firewall
  body: json
  auth: bearer
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}
