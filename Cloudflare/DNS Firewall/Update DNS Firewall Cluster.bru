meta {
  name: Update DNS Firewall Cluster
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/accounts/:account_id/dns_firewall/:dns_firewall_id
  body: json
  auth: bearer
}

params:path {
  dns_firewall_id: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}
