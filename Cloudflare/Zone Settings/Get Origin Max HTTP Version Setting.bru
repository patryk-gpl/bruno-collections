meta {
  name: Get Origin Max HTTP Version Setting
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/zones/:zone_id/settings/origin_max_http_version
  body: none
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
