meta {
  name: Get zone setting
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/zones/:zone_id/settings/:setting_id
  body: none
  auth: bearer
}

params:path {
  zone_id: 
  setting_id: 
}

auth:bearer {
  token: {{token}}
}
