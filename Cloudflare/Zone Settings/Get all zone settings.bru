meta {
  name: Get all zone settings
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/zones/:zone_id/settings
  body: none
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
