meta {
  name: Get Origin H2 Max Streams Setting
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/zones/:zone_id/settings/origin_h2_max_streams
  body: none
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
