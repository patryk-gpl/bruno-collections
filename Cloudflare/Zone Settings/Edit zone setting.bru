meta {
  name: Edit zone setting
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/zones/:zone_id/settings/:setting_id
  body: json
  auth: bearer
}

params:path {
  zone_id: 
  setting_id: 
}

auth:bearer {
  token: {{token}}
}
