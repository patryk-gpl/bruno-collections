meta {
  name: Change Cloudflare Fonts setting
  type: http
  seq: 8
}

patch {
  url: {{baseUrl}}/zones/:zone_id/settings/fonts
  body: json
  auth: apikey
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
