meta {
  name: Get Cloudflare Fonts setting
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/zones/:zone_id/settings/fonts
  body: none
  auth: apikey
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
