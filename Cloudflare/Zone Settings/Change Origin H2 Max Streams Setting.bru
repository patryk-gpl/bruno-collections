meta {
  name: Change Origin H2 Max Streams Setting
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/zones/:zone_id/settings/origin_h2_max_streams
  body: json
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "value": 0
  }
}
