meta {
  name: Edit multiple zone settings
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/zones/:zone_id/settings
  body: json
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  [
    {}
  ]
}
