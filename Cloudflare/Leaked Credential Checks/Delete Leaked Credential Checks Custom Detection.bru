meta {
  name: Delete Leaked Credential Checks Custom Detection
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/zones/:zone_id/leaked-credential-checks/detections/:detection_id
  body: none
  auth: apikey
}

params:path {
  zone_id: 
  detection_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
