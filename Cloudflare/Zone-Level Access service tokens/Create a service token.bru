meta {
  name: Create a service token
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/zones/:zone_id/access/service_tokens
  body: json
  auth: apikey
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
