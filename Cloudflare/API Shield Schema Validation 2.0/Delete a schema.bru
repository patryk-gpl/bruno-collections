meta {
  name: Delete a schema
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/zones/:zone_id/api_gateway/user_schemas/:schema_id
  body: none
  auth: apikey
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
