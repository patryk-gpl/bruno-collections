meta {
  name: Delete domain
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/pages/projects/:project_name/domains/:domain_name
  body: json
  auth: bearer
}

params:path {
  domain_name: 
  project_name: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}
