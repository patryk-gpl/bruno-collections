meta {
  name: Update a Bookmark application
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/accounts/:account_id/access/bookmarks/:bookmark_id
  body: json
  auth: apikey
}

params:path {
  bookmark_id: 
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
