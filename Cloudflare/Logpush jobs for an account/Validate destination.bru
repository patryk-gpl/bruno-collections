meta {
  name: Validate destination
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/accounts/:account_id/logpush/validate/destination
  body: json
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "destination_conf": ""
  }
}
