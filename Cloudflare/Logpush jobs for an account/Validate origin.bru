meta {
  name: Validate origin
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/accounts/:account_id/logpush/validate/origin
  body: json
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "logpull_options": ""
  }
}
