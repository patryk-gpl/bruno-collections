meta {
  name: Update a device managed network
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/accounts/:account_id/devices/networks/:network_id
  body: json
  auth: apikey
}

params:path {
  network_id: 
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "config": {},
    "name": "",
    "type": ""
  }
}
