meta {
  name: Delete a device managed network
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/devices/networks/:network_id
  body: json
  auth: apikey
}

params:path {
  network_id: 
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
