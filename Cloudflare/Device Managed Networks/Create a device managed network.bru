meta {
  name: Create a device managed network
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/devices/networks
  body: json
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "config": {},
    "name": "",
    "type": ""
  }
}
