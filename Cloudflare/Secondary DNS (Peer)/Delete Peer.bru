meta {
  name: Delete Peer
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/secondary_dns/peers/:peer_id
  body: json
  auth: bearer
}

params:path {
  peer_id: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}
