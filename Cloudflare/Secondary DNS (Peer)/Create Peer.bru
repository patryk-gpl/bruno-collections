meta {
  name: Create Peer
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/secondary_dns/peers
  body: json
  auth: bearer
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": ""
  }
}
