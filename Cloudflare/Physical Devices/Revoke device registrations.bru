meta {
  name: Revoke device registrations
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/accounts/:account_id/devices/physical-devices/:device_id/revoke
  body: none
  auth: bearer
}

params:path {
  account_id: 
  device_id: 
}

auth:bearer {
  token: {{token}}
}
