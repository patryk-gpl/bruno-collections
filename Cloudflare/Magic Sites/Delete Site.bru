meta {
  name: Delete Site
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/magic/sites/:site_id
  body: json
  auth: apikey
}

params:path {
  site_id: 
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
