meta {
  name: Delete buckets for full packet captures
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/accounts/:account_id/pcaps/ownership/:ownership_id
  body: none
  auth: apikey
}

params:path {
  ownership_id: 
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
