meta {
  name: Add buckets for full packet captures
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/accounts/:account_id/pcaps/ownership
  body: json
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "destination_conf": ""
  }
}
