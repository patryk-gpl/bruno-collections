meta {
  name: Disable Outgoing Zone Transfers
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/zones/:zone_id/secondary_dns/outgoing/disable
  body: json
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
