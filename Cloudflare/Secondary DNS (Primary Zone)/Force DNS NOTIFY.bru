meta {
  name: Force DNS NOTIFY
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/zones/:zone_id/secondary_dns/outgoing/force_notify
  body: json
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
