meta {
  name: Update Primary Zone Configuration
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/zones/:zone_id/secondary_dns/outgoing
  body: json
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
