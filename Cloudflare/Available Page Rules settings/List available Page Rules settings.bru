meta {
  name: List available Page Rules settings
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/zones/:zone_id/pagerules/settings
  body: none
  auth: bearer
}

params:path {
  zone_id: 
}

auth:bearer {
  token: {{token}}
}
