meta {
  name: Delete a Warp Connector Tunnel
  type: http
  seq: 14
}

delete {
  url: {{baseUrl}}/accounts/:account_id/warp_connector/:tunnel_id
  body: json
  auth: apikey
}

params:path {
  account_id: 
  tunnel_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {}
}
