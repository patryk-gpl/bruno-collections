meta {
  name: Get a Cloudflare Tunnel token
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/accounts/:account_id/cfd_tunnel/:tunnel_id/token
  body: none
  auth: apikey
}

params:path {
  account_id: 
  tunnel_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
