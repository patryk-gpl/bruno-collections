meta {
  name: Create a Warp Connector Tunnel
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/accounts/:account_id/warp_connector
  body: json
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {
    "name": ""
  }
}
