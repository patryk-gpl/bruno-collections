meta {
  name: Create watermark profiles via basic upload
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/stream/watermarks
  body: multipartForm
  auth: apikey
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:multipart-form {
  file: 
  name: 
  opacity: 
  padding: 
  position: 
  scale: 
}
