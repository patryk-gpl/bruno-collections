meta {
  name: Create your Zero Trust organization
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/zones/:zone_id/access/organizations
  body: json
  auth: apikey
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
