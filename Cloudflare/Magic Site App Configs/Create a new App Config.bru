meta {
  name: Create a new App Config
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/magic/sites/:site_id/app_configs
  body: json
  auth: apikey
}

params:path {
  account_id: 
  site_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}

body:json {
  {}
}
