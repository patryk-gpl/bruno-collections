meta {
  name: Get dataset CSV stream
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/radar/datasets/:alias
  body: none
  auth: apikey
}

params:path {
  alias: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
