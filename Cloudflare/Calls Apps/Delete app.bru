meta {
  name: Delete app
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/accounts/:account_id/calls/apps/:app_id
  body: none
  auth: bearer
}

params:path {
  app_id: 
  account_id: 
}

auth:bearer {
  token: {{token}}
}
