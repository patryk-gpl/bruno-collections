meta {
  name: Create a new app
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/:account_id/calls/apps
  body: json
  auth: bearer
}

params:path {
  account_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": ""
  }
}
