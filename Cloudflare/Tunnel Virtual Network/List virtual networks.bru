meta {
  name: List virtual networks
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/accounts/:account_id/teamnet/virtual_networks
  body: none
  auth: apikey
}

params:query {
  ~id: 
  ~name: 
  ~is_default: 
  ~is_default_network: 
  ~is_deleted: 
}

params:path {
  account_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
