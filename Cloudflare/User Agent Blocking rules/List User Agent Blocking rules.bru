meta {
  name: List User Agent Blocking rules
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/zones/:zone_id/firewall/ua_rules
  body: none
  auth: apikey
}

params:query {
  ~page: 
  ~description: 
  ~per_page: 
  ~user_agent: 
  ~paused: 
}

params:path {
  zone_id: 
}

headers {
  X-Auth-Email: {{apiKey}}
}

auth:apikey {
  key: X-Auth-Email
  value: {{apiKey}}
  placement: header
}
